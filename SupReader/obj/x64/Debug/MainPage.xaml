<Page
    x:Class="SupReader.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SupReader"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:SupReader.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <SplitView DisplayMode="Inline" IsPaneOpen="True">
            <SplitView.Pane>
                <controls:Nav1 x:ConnectionId='2' x:Name="TheNavPane1"/>
            </SplitView.Pane>
            
            <SplitView.Content>
                <controls:Content x:ConnectionId='3' x:Name="TheContent"/>
            </SplitView.Content>
        </SplitView>
    </Grid>
    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Content="Refresh" Icon="RepeatAll" />
            <AppBarButton x:ConnectionId='4' x:Name="SettingsButton" Icon="Setting"                              />
        </CommandBar>
    </Page.BottomAppBar>
</Page>

