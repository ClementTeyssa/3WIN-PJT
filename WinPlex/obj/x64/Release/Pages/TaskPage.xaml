<Page
    x:Class="WinPlex.Pages.TaskPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinPlex.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel>
            <AppBarButton x:ConnectionId='2' Icon="Back" Style="{StaticResource NavigationBackButtonNormalStyle}"                           />
            <ScrollViewer>
                <StackPanel>
                    <TextBlock Text="Tâches"  Margin="15" FontSize="35" FontFamily="Georgia" HorizontalAlignment="Center"/>
                    <ListView x:ConnectionId='3' x:Name="taskListView" ItemsSource="{Binding ListTasks}"                                           >
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                                    <Border>
                                        <Grid Margin="5" Width="100">
                                            <TextBlock Text="{Binding completed}"/>
                                            <TextBlock Text="{Binding priority}"/>
                                        </Grid>
                                    </Border>
                                    <Border>
                                        <Grid Margin="5">
                                            <TextBlock Text="{Binding content}"/>
                                        </Grid>
                                    </Border>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </ScrollViewer>
        </StackPanel>
    </Grid>
</Page>

